# This is a basic workflow to help you get started with Actions

name: Compress to .mcpack

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      
      - name: add all files to zip
        run: zip -rv bedwarsultimatetexturepack.zip manifest.json pack_icon.png textures/
        
      - name: display all files for debugging
        run: ls
        
      - name: test for and create directory
        run: |
         chmod +x ./build/*
         ./build/prepare-dir-mcpack.sh
         
      - name: Move file and rename
        run: mv bedwarsultimatetexturepack.zip ./texture-packs/bedwarsultimatetexturepack.mcpack
        
      - name: Upload a Build Artifact
        uses: actions/upload-artifact@v2.2.3
        with:
         path: texture-packs/bedwarsultimatetexturepack.mcpack
